import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MapScreen = () => {
  return (
    <View>
      <Text>Here the map stuff..</Text>
    </View>
  );
};

export default MapScreen;

const styles = StyleSheet.create({});
